diff -ru chartit/charts.py chartit-patched/charts.py
--- chartit/charts.py	2015-07-12 16:06:32.000000000 -0400
+++ chartit-patched/charts.py	2015-07-12 16:05:40.195241961 -0400
@@ -327,7 +327,7 @@
         for term, options in self.series_options.items():
             dss = self.datasource.series
             for lv in dss[term]['_lv_set']:
-                data = [dss[term]['_cv_lv_dfv'][cv].get(lv, None) for cv 
+                data = [dss[term]['_cv_lv_dfv'][cv].get(lv, 0) for cv 
                         in cv_raw]
                 #name = '-'.join(dstd['legend_by']) + ":" + "-".join(lv)
                 term_pretty_name = term.replace('_', ' ')
diff -ru chartit/templatetags/chartit.py chartit-patched/templatetags/chartit.py
--- chartit/templatetags/chartit.py	2015-07-12 16:06:32.000000000 -0400
+++ chartit-patched/templatetags/chartit.py	2015-07-12 16:06:11.027241102 -0400
@@ -1,7 +1,8 @@
 from itertools import izip_longest
 
 from django import template
-from django.utils import simplejson
+# from django.utils import simplejson
+import simplejson
 from django.utils.safestring import mark_safe
 from django.conf import settings
 import posixpath
@@ -71,4 +72,4 @@
                                         CHART_LOADER_URL))
     else:
         embed_script = embed_script %((), CHART_LOADER_URL)
-    return mark_safe(embed_script)
\ No newline at end of file
+    return mark_safe(embed_script)
diff -ru chartit/validation.py chartit-patched/validation.py
--- chartit/validation.py	2015-07-12 16:06:32.000000000 -0400
+++ chartit-patched/validation.py	2015-07-12 16:05:40.195241961 -0400
@@ -47,7 +47,11 @@
         field_details = model._meta.get_field_by_name(terms[0])
         # if the field is direct field
         if field_details[2]:
-            m = field_details[0].related.parent_model
+            # m = field_details[0].related.parent_model
+            try:
+                m = field_details[0].related.parent_model
+            except:
+                m = field_details[0].related.model
         else:
             m = field_details[0].model
         
