diff -ru chartit/templatetags/chartit.py chartit-patch/templatetags/chartit.py
--- chartit/templatetags/chartit.py	2015-06-10 21:14:18.000000000 -0400
+++ chartit-patch/templatetags/chartit.py	2015-06-10 21:08:38.707394671 -0400
@@ -1,7 +1,8 @@
 from itertools import izip_longest
 
 from django import template
-from django.utils import simplejson
+# from django.utils import simplejson
+import simplejson
 from django.utils.safestring import mark_safe
 from django.conf import settings
 import posixpath
@@ -71,4 +72,4 @@
                                         CHART_LOADER_URL))
     else:
         embed_script = embed_script %((), CHART_LOADER_URL)
-    return mark_safe(embed_script)
\ No newline at end of file
+    return mark_safe(embed_script)
diff -ru chartit/validation.py chartit-patch/validation.py
--- chartit/validation.py	2015-06-10 21:14:18.000000000 -0400
+++ chartit-patch/validation.py	2015-06-10 21:10:20.579395875 -0400
@@ -47,7 +47,11 @@
         field_details = model._meta.get_field_by_name(terms[0])
         # if the field is direct field
         if field_details[2]:
-            m = field_details[0].related.parent_model
+            # m = field_details[0].related.parent_model
+            try:
+                m = field_details[0].related.parent_model
+            except:
+                m = field_details[0].related.model
         else:
             m = field_details[0].model
         
